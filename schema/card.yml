card_lex:
  type: string
  required: true
  coerce: str

rarity:
  type: string
  nullable: true
  allowed: ['Common', 'Uncommon', 'Rare', 'Epic', 'Legendary', 'Token']

artist:
  type: string
  nullable: true

appearance:
  type: list
  schema:
    type: string

flavor:
  type: string
  nullable: true

oracle_id:
  type: string
  required: true

set_id:
  type: string
  required: true
